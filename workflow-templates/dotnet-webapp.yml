name: .NET Web App

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  build:
    uses: TaxSystems/shared-workflows/.github/workflows/dotnet-webapp.yml@master
    with:
      dotnet-version: '8.0.x'
