name: Node.js Function App

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  build:
    uses: TaxSystems/shared-workflows/.github/workflows/nodejs-functionapp.yml@master
    with:
      node-version: '22.x'
