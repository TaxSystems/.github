name: Angular Web App

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  build:
    uses: TaxSystems/shared-workflows/.github/workflows/angular-webapp.yml@master
    with:
      node-version: '22.x'
