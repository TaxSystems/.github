name: Terraform Module

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  validate:
    uses: TaxSystems/shared-workflows/.github/workflows/terraform-module.yml@master
