name: .NET NuGet Package

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

jobs:
  build:
    uses: TaxSystems/shared-workflows/.github/workflows/dotnet-nuget.yml@master
    with:
      dotnet-version: '8.0.x'
